### **15 Exercícios Teóricos sobre `console.log()` em JavaScript**  

#### **1. Conceitos Básicos**  
1. **O que é a função `console.log()` em JavaScript?**  
2. **Qual é a principal finalidade de usar `console.log()`?**  
3. **O ponto e vírgula (`;`) no final do `console.log()` é obrigatório?**  

#### **2. Sintaxe e Uso**  
4. **Qual é a sintaxe correta para exibir a mensagem "Olá, mundo!" no console?**  
5. **Como exibir um número (ex.: 42) usando `console.log()`?**  
6. **O que acontece se você tentar exibir um texto sem aspas no `console.log()`?**  

#### **3. Tipos de Dados**  
7. **Qual é a diferença entre `console.log(10)` e `console.log("10")`?**  
8. **Como exibir um número decimal (ex.: 3.14) corretamente?**  
9. **Se um texto contém aspas simples (ex.: `'JavaScript'`), como você o exibe dentro de um `console.log()`?**  

#### **4. Múltiplos Valores**  
10. **Como exibir três valores (um número, um texto e um decimal) em uma única chamada de `console.log()`?**  
11. **O que acontece se você usar vírgulas para separar valores dentro do `console.log()`?**  

#### **5. Debug e Console**  
12. **Onde a saída do `console.log()` aparece quando executado em um navegador?**  
13. **O `console.log()` é visível para o usuário final de um site?**  

#### **6. Casos Especiais**  
14. **Qual é a diferença entre usar aspas simples (`' '`), aspas duplas (`" "`) e crases (`` ` ` ``) no `console.log()`?**  
15. **Por que às vezes o `console.log()` exibe um erro quando usamos aspas dentro de um texto?**  

---  

### **Gabarito**  

1. **Resposta:** É uma função/método usado para exibir mensagens ou valores no console, principalmente para debug.  
2. **Resposta:** Verificar valores, debugar código e acompanhar a execução do programa.  
3. **Resposta:** Não, mas é recomendado para boas práticas.  
4. **Resposta:** `console.log("Olá, mundo!");`  
5. **Resposta:** `console.log(42);` (sem aspas).  
6. **Resposta:** O JavaScript tentará interpretar como variável ou código, gerando erro se não existir.  
7. **Resposta:** O primeiro é um número (`number`), o segundo é uma string (`string`).  
8. **Resposta:** `console.log(3.14);` (usando ponto, não vírgula).  
9. **Resposta:** Usando aspas duplas: `console.log("'JavaScript'");`  
10. **Resposta:** `console.log(10, "Texto", 5.5);`  
11. **Resposta:** Os valores são exibidos na mesma linha, separados por espaço.  
12. **Resposta:** Na aba "Console" das Ferramentas do Desenvolvedor (F12).  
13. **Resposta:** Não, só é visível no console.  
14. **Resposta:** Aspas simples e duplas são equivalentes para strings; crases permitem **template strings** (interpolação).  
15. **Resposta:** Porque aspas internas podem fechar a string prematuramente (ex.: `console.log('It's an error');`). Usar aspas opostas resolve (ex.: `console.log("It's correct");`).  

---  
**Dica:** Pratique no console do navegador ou no Node.js para fixar! 😊