Aqui está a versão refinada em **Markdown (MD)**, com formatação otimizada para legibilidade e consistência:

```markdown
## Exercícios de JavaScript + HTML

### 1. Como vincular um arquivo JavaScript externo a um HTML?  
a) `<script href="script.js"></script>`  
b) `<script src="script.js"></script>`   
c) `<javascript src="script.js"></javascript>`  
d) `<link rel="script" href="script.js">`  

### 2. Onde é recomendado colocar a tag `<script>` no HTML?  
a) No `<head>` para carregar rápido.  
b) No final do `<body>` para melhor performance.  
c) Dentro de um comentário HTML.  
d) Em um arquivo CSS.  

### 3. Qual função exibe uma mensagem no console do navegador?  
a) `alert()`  
b) `console.log()`   
c) `document.write()`  
d) `print()`  

### 4. O que acontece se executarmos `alert("Olá")` no Node.js?  
a) Exibe um pop-up no terminal.  
b) Funciona normalmente.  
c) Gera um erro (`alert is not defined`).   
d) Nenhuma das anteriores.  

### 5. Qual é a principal vantagem de usar um arquivo JS externo?  
a) Reduzir o tamanho do HTML.  
b) Organização e reutilização de código.  
c) Melhorar o SEO do site.  
d) Todas as anteriores.   

### 6. Como criar um comentário em JavaScript dentro de um arquivo HTML?  
a) `<!-- Comentário -->` (HTML)  
b) `// Comentário` (JS)  
c) `/* Comentário */` (JS)  
d) Tanto **b** quanto **c**.   

### 7. Qual método altera o conteúdo de um elemento HTML via JavaScript?  
a) `document.getElementByTagName()`  
b) `console.log()`  
c) `innerHTML`   
d) `window.alert()`  

### 8. O que o código abaixo faz?  
```html
<script>
    document.write("Bem-vindo!");
</script>
```  
a) Exibe "Bem-vindo!" no console.  
b) Substitui todo o HTML pelo texto.   
c) Cria um pop-up.  
d) Nenhuma das anteriores.  

### 9. Qual é a extensão correta para um arquivo JavaScript?  
a) `.java`  
b) `.js`   
c) `.javascript`  
d) `.script`  

### 10. Para que serve o atributo `defer` na tag `<script>`?  
a) Executar o script antes do HTML carregar.  
b) Adiar a execução do script até após o HTML carregar.   
c) Evitar que o script seja executado.  
d) Nenhuma das anteriores.  

---

### 🔹 Gabarito  
| Questão | Resposta Correta |  
|---------|------------------|  
| 1       | b                |  
| 2       | b                |  
| 3       | b                |  
| 4       | c                |  
| 5       | d                |  
| 6       | d                |  
| 7       | c                |  
| 8       | b                |  
| 9       | b                |  
| 10      | b                |  